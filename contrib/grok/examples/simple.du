// Simple one-shot query example
grok = require("grok")

// Basic query with defaults
response = grok.prompt("What is Duso?")
print(response)
print("")

// Query with custom temperature
response = grok.prompt("Write a creative story about a robot", {
  temperature = 0.9,
  max_tokens = 500
})
print(response)
